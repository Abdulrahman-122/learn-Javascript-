- Functions
    <--  it's a way to make code clear + prevent any replicates for the code.
    <-- we can make hoisting into functions -> you call the function before it's defintion in js

  greetWorld(); // Output: Hello, World!

function greetWorld() {
  console.log('Hello, World!');
}
--
this is how to call a function and make it
function sayThanks(){
  console.log('Thank you for your purchase! We appreciate your business.')
}
sayThanks();
---
this is how to pass a parameter to the function ;
let name;
function sayThanks(name) {
  console.log('Thank you for your purchase, '+ name + '! We appreciate your business.'
);
}
sayThanks('Cole')
--
to make default parameter ;
function greeting (name = 'stranger') {
  console.log(`Hello, ${name}!`)
}

greeting('Nick') // Output: Hello, Nick!
greeting() // Output: Hello, stranger!
---
if you want to return a value from a function ;
function rectangleArea(width, height) {
  if (width < 0 || height < 0) {
    return 'You need positive integers to calculate area!';
  }
  return width * height;
}
---
you need to divide the function into more function to make your code more easy
function multiplyByNineFifths(number) {
  return number * (9/5);
};

function getFahrenheit(celsius) {
  return multiplyByNineFifths(celsius) + 32;
};

getFahrenheit(15); // Returns 59
-- 
this is how to make anonymous function 
const day='Wednesday';
const plantNeedsWater=function (day){
  if (day==='Wednesday'){
    return true ;
  }
  else{
    return false;
  }
}
console.log(plantNeedsWater('Tuesday'));

--
this is how  to make an arrow function
const rectangleArea = (width, height) => {
  let area = width * height;
  return area;
};
---
concise arrow functions


there are there cases
1. if there zero parameter for the function
const variblename= () => {body};
2. if there is one  parameter for the function
const variblename= parameter  => {body};
3. if there are multiple  parameters for the function
const variblename= (par1,par2) => {body};


notes;
const variableName=(par1) => {body with return statements} ==> const variableName=par1 => body without return
like this example;
const squareNum = (num) => {
  return num * num;
};
const squareNum = num => num * num;
